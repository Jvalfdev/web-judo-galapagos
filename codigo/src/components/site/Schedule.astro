<section id="horarios" class="section section--schedule" aria-labelledby="schedule-title">
	<div class="section__head section__head--row">
		<div>
			<p class="section__eyebrow">Horarios</p>
			<h2 id="schedule-title" class="section__title">Cuándo entrenamos</h2>
			<p class="section__intro section__intro--narrow">
				<strong>Martes y jueves.</strong> Franjas por edad orientativas (confirma reparto y cupo con el
				maestro). Otros días o cambios de temporada: pregúntale en el tatami o por contacto.
			</p>
		</div>
		<a href="#contacto" class="btn-mini">Contacto</a>
	</div>

	<div class="schedule" role="region" aria-label="Horarios martes y jueves">
		<p class="schedule__caption">Horario habitual</p>
		<div class="schedule__head">
			<span class="schedule__col schedule__col--group">Grupo</span>
			<span class="schedule__col">Martes</span>
			<span class="schedule__col">Jueves</span>
		</div>
		<ul class="schedule__body">
			<li class="schedule__row">
				<span class="schedule__group">
					<strong>Peques</strong>
					<small>Niños más pequeños; el rango de edad lo concretamos en el tatami según nivel y plaza.</small>
				</span>
				<span class="schedule__cell" data-day="Martes">17:15–18:00</span>
				<span class="schedule__cell" data-day="Jueves">17:15–18:00</span>
			</li>
			<li class="schedule__row">
				<span class="schedule__group">
					<strong>Infantiles</strong>
					<small>Grupo infantil intermedio.</small>
				</span>
				<span class="schedule__cell" data-day="Martes">18:00–19:00</span>
				<span class="schedule__cell" data-day="Jueves">18:00–19:00</span>
			</li>
			<li class="schedule__row schedule__row--accent">
				<span class="schedule__group">
					<strong>Cadetes y adultos</strong>
					<small>Orientativo desde cadete; adolescentes y adultos comparten franja.</small>
				</span>
				<span class="schedule__cell" data-day="Martes">19:00–20:00</span>
				<span class="schedule__cell" data-day="Jueves">19:00–20:00</span>
			</li>
		</ul>
	</div>
</section>

<style>
	.section__head--row {
		display: flex;
		flex-wrap: wrap;
		align-items: flex-end;
		justify-content: space-between;
		gap: var(--space-md);
		max-width: none !important;
		margin-bottom: var(--space-lg);
	}

	.section__intro--narrow {
		max-width: 460px;
		margin-top: var(--space-sm);
	}

	.btn-mini {
		display: inline-flex;
		align-items: center;
		height: 2.6rem;
		padding: 0 1.2rem;
		border-radius: var(--radius-full);
		font-size: 0.88rem;
		font-weight: 600;
		text-decoration: none;
		color: var(--btn-on-accent);
		background: linear-gradient(135deg, var(--accent), #c9183a);
		border: 1px solid rgba(255, 255, 255, 0.14);
		box-shadow: 0 10px 36px -14px var(--accent-glow);
		transition:
			transform 0.25s var(--ease-out-expo),
			filter 0.25s ease;
	}

	.btn-mini:hover {
		filter: brightness(1.06);
		transform: translateY(-2px);
	}

	.schedule {
		border-radius: calc(var(--radius-lg) + 6px);
		border: 1px solid var(--border-subtle);
		background: var(--schedule-wrap-bg);
		backdrop-filter: blur(16px);
		overflow: hidden;
	}

	.schedule__caption {
		margin: 0;
		padding: 0.85rem 1rem 0.35rem;
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.055em;
		text-transform: uppercase;
		color: var(--ice);
		opacity: 0.9;
		border-bottom: 1px solid var(--border-subtle);
		background: var(--schedule-caption-bg);
	}

	.schedule__head {
		display: none;
		padding: 0.65rem 1rem;
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.055em;
		text-transform: uppercase;
		color: var(--text-muted);
		border-bottom: 1px solid var(--border-subtle);
		background: var(--schedule-head-bg);
	}

	@media (min-width: 880px) {
		.schedule__head {
			display: grid;
			grid-template-columns: 2fr repeat(2, 1fr);
			gap: 0.75rem;
			align-items: center;
		}
	}

	.schedule__body {
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.schedule__row {
		display: grid;
		grid-template-columns: 1fr;
		gap: 0.75rem;
		padding: var(--space-md) 1rem;
		border-bottom: 1px solid var(--border-subtle);
		transition: background 0.25s ease;
	}

	.schedule__row:last-child {
		border-bottom: none;
	}

	.schedule__row:hover {
		background: var(--schedule-row-hover);
	}

	@media (min-width: 880px) {
		.schedule__row {
			grid-template-columns: 2fr repeat(2, 1fr);
			align-items: center;
			gap: 0.75rem;
			padding: 1rem 1rem;
		}
	}

	.schedule__row--accent {
		position: relative;
		background: var(--schedule-row-accent-bg);
	}

	.schedule__row--accent::before {
		content: "";
		position: absolute;
		left: 0;
		top: 0;
		bottom: 0;
		width: 3px;
		background: linear-gradient(to bottom, var(--accent), transparent);
		border-radius: 3px;
		opacity: 0.85;
	}

	.schedule__group {
		display: flex;
		flex-direction: column;
		gap: 0.2rem;
	}

	.schedule__group strong {
		font-family: var(--font-display);
		font-weight: 700;
		font-size: 1rem;
		letter-spacing: -0.018em;
	}

	.schedule__group small {
		font-size: 0.82rem;
		color: var(--text-muted);
	}

	.schedule__cell {
		font-variant-numeric: tabular-nums;
		font-size: 0.92rem;
		font-weight: 600;
		color: var(--schedule-cell-text);
		padding: 0.35rem 0;
	}

	@media (min-width: 880px) {
		.schedule__cell {
			padding: 0;
		}
	}

	@media (max-width: 879px) {
		.schedule__cell[data-day]::before {
			content: attr(data-day);
			display: block;
			font-size: 0.68rem;
			font-weight: 700;
			letter-spacing: 0.055em;
			text-transform: uppercase;
			color: var(--text-muted);
			margin-bottom: 0.15rem;
		}
	}
</style>
